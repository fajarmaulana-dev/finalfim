import { VToastProps } from './types';
export declare const useToast: () => {
    show: (message: string, options?: Partial<VToastProps>) => void;
    reset: () => void;
    confirm: (message: string, options: Partial<VToastProps>) => void;
    confirmDelete: (message: string, options: Partial<VToastProps>) => void;
};
