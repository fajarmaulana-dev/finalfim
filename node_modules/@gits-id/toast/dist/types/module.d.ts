import { Module } from 'vuex';
import type { VToastProps } from './types';
export interface ToastState {
    show: boolean;
    message: string;
    options: VToastProps;
}
export declare const toastModule: <R>() => Module<ToastState, R>;
